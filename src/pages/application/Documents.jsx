import React,{useState}from'react';import{useNavigate}from'react-router-dom';import ApplicationLayout from'../../components/ApplicationLayout';import'./Documents.css';export default function Documents(){const n=useNavigate(),[files,setFiles]=useState({});const docs=['Passport Photo','Category Certificate','Academic Documents','Teaching Experience Certificate','Administrative Experience Certificate','Research Papers','Publications','Patents / Awards','Payment Receipt'];return <ApplicationLayout title="Document Upload Center"><div className="form-card documents-page"><p className="section-note">Upload clear files. Each item is kept separately for admin verification.</p><div className="document-grid">{docs.map(d=><div className="upload-box" key={d}><b>{d}</b><p>{files[d]?.name||'No file selected'}</p><input type="file" onChange={e=>setFiles({...files,[d]:e.target.files[0]})}/></div>)}</div><div className="form-actions"><button className="btn btn-outline">Save Draft</button><button className="btn btn-primary" onClick={()=>n('/application/payment')}>Continue</button></div></div></ApplicationLayout>}