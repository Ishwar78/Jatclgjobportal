import React,{useState}from'react';import{useNavigate}from'react-router-dom';import ApplicationLayout from'../../components/ApplicationLayout';import'./Research.css';export default function Research(){const n=useNavigate(),[f,setF]=useState({papers:0,books:0,chapters:0,projects:0,patents:0,awards:0,lectures:0});const total=Object.values(f).reduce((a,v)=>a+Number(v||0),0);return <ApplicationLayout title="Research Score"><div className="form-card research-page"><p className="section-note">Enter count for research activities. Final eligibility scoring can be verified by the recruitment administrator.</p><div className="form-grid">{Object.entries({papers:'Research Papers',books:'Books',chapters:'Book Chapters',projects:'Research Projects',patents:'Patents',awards:'Awards',lectures:'Invited Lectures'}).map(([k,l])=><div key={k}><label className="label">{l}</label><input className="form-control" type="number" min="0" value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></div>)}<div className="score-box full">Research Activities Total: {total}</div></div><div className="form-actions"><button className="btn btn-outline">Save Draft</button><button className="btn btn-primary" onClick={()=>n('/application/documents')}>Continue</button></div></div></ApplicationLayout>}